version: '3.9'

services:
  postgres:
    container_name: sql-functions-postgres
    restart: unless-stopped
    image: postgres:14-alpine
    expose:
      - 5432
    ports:
      - 35432:5432
    env_file: .env
